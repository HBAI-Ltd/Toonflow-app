# 手动媒体模式移除项目级模型选择计划（当前任务）

## 任务卡

在只输出完整图片/视频提示词、由用户在外部工具生成媒体的模式下，新建和编辑项目不再展示或要求选择图片模型、图片质量、视频模型及视频模式；没有配置媒体模型的项目仍可正常进入生产流程和视频工作台。

## 影响分析

- 项目弹窗当前展示四项媒体模型配置，并在提交时强制校验，需删除对应 UI 和校验。
- 项目列表当前在模型为空或供应商不可用时强制打开编辑弹窗，需取消媒体供应商门禁。
- 生产 Agent 当前会读取视频供应商并在模型为空时抛错，需改为手动媒体上下文，不访问图片/视频模型供应商。
- 视频工作台数据加载当前拒绝未配置视频模型的项目；新增轨道也有一次无实际用途的供应商读取，均需取消。
- 项目数据库字段和 API 请求字段继续保留空字符串兼容，避免数据库迁移或破坏旧客户端。

## 计划修改文件

### 前端手动媒体工作树

- `src/views/project/components/projectDialog.vue`
- `src/views/project/index.vue`
- `tests/manualProjectModelSelection.test.mjs`（新增）

### 后端手动媒体工作树

- `src/lib/manualMediaMode.ts`
- `src/agents/productionAgent/index.ts`
- `src/routes/production/workbench/getGenerateData.ts`
- `src/routes/production/workbench/addTrack.ts`
- `tests/manualMediaMode.test.mjs`
- `PLAN.md`
- `DEV_REPORT.md`
- `CODEX_REVIEW.md`
- `data/web/index.html`（前端构建产物）
- `data/serve/app.js`（后端构建产物）

### 当前本地运行副本

- 同步上述后端源文件、测试和交付文档。
- 同步 `data/web/` 与 `data/serve/app.js` 并重启当前 Toonflow 服务。

## 实施顺序

1. 先新增前后端静态回归测试，覆盖项目弹窗无模型选择、无必选校验、项目可直接进入、生产 Agent 不访问媒体供应商、工作台与轨道不要求视频模型。
2. 最小修改项目弹窗、项目进入门禁和三个后端媒体模型依赖点。
3. 运行前后端相关测试、前端 lint/type-check、前端生产构建、后端 TypeScript lint 与生产构建。
4. 将构建产物部署到当前运行副本，重启 Toonflow，确认页面启动且新建项目弹窗不再渲染红框区域。
5. 更新开发报告与自查报告；更新既有草稿 PR，等待人工合并。

## 验收标准

- 新建/编辑项目弹窗不显示图片模型、图片质量、视频模型或视频模式。
- 不选择媒体模型也能创建/编辑项目并进入项目。
- 生产 Agent 不再解析项目媒体模型或访问媒体供应商，只使用手动媒体提示。
- 无视频模型的项目可以加载视频工作台并新增轨道。
- 画幅比例、视觉手册、导演手册和其他既有项目字段保持不变。
- 前后端测试、lint 和生产构建通过，当前 Toonflow 页面可启动。

## 风险与回滚

- 风险：视频工作台内部仍保留按需要选择具体视频模型/模式来组织参考素材的能力；本任务只移除截图所示的项目级必选配置，不擅自删除工作台能力。
- 风险：旧项目保存过的模型字段继续留在数据库中，但生产 Agent 不再依赖它们；这是兼容性保留，不会触发媒体生成。
- 回滚：恢复上述源码与构建产物并重启服务；无数据库迁移，无用户数据回滚。

## 监督状态

- [x] 已读取当前任务卡和截图。
- [x] 已确认前后端手动媒体工作树均位于 `agent/manual-media-upload` 非主分支且工作区干净。
- [x] 已完成影响分析并列出计划修改文件。
- [x] 已获得人工确认并完成测试和代码修改。
- [x] 已运行前后端测试、后端 lint、前后端构建与本地页面启动验证。
- [x] 已更新报告；本次变更将推送到既有草稿 PR，最终由人工合并。

---

# 手动图片/视频生成交互计划（原任务记录）

## 任务卡

- 保留文本模型生成/整理提示词。
- 所有图片和视频入口只输出完整提示词，不调用媒体模型。
- 用户在外部生成后回到原位置上传，继续使用原素材、分镜和视频选择逻辑。
- 不修改其他业务逻辑，不新增依赖，不改数据库结构。

## 允许修改范围

- 后端媒体生成 API 拦截和 AI 调用层硬保护。
- 新增统一手动媒体模式常量与防误调用保护。
- 新增视频上传并绑定原轨道的 API。
- 前端资产、分镜、图片编辑和视频工作台中的生成按钮、提示词复制和上传交互。
- 供应商图片/视频模型测试弹窗改为提示词复制和结果文件上传。
- 对应测试、API 文档以及 `data/serve/app.js`、`data/web/index.html` 构建产物。

## 验收

1. 主流程不再请求图片/视频生成 API。
2. 旧客户端请求也会在模型调用前被拒绝。
3. 完整提示词可单条或批量复制。
4. 图片和视频均能回传到原位置并沿用后续逻辑。
5. 前后端 lint、测试、构建和页面启动检查通过。

# 豆包网页视频 API 接入计划

# 东北 90 年代拟人猫视觉手册新增计划

## 当前任务卡

- 目标：在 Toonflow 新建一套“东北 90 年代拟人猫电影动画”视觉手册。
- 故事范围：1988—1990 年，虚构城市“北安市红星机械厂家属院”。
- 单集时长：每个视频约 60 秒；“90 年代”仅表示故事时代，不表示 90 秒时长。
- 叙事比例：50% 家庭与伙伴喜剧、30% 年代回忆、20% 温情成长。
- 视觉定位：轻写实电影级拟人猫动画，保留真实猫毛与猫脸结构，突出东北老工业城市、国企家属院和真实生活使用痕迹。
- 固定角色：小橘、橘爸、花妈、猫奶奶、大勇；角色识别特征、服饰、比例与关系不得漂移。
- 时代红线：禁止 1990 年后明显现代物品、精装修、现代包装与现代电子设备进入画面。

## 现存视觉手册分析

Toonflow 当前每套完整视觉手册固定包含 12 个 Markdown 文件：

1. `README.md`：风格定位、适用范围、严禁内容、体验说明。
2. `prefix.md`：全局风格基因、色盘、光影、硬约束与负向规则。
3. `art_prompt/art_character.md`：角色基础形象和设定图规范。
4. `art_prompt/art_character_derivative.md`：服装、状态和造型衍生规则。
5. `art_prompt/art_prop.md`：道具分类、年代、材质和设定图规则。
6. `art_prompt/art_prop_derivative.md`：使用、磨损、季节和叙事状态变体。
7. `art_prompt/art_scene.md`：室内外场景、季节、空间与主视图规则。
8. `art_prompt/art_scene_derivative.md`：景别、时段、天气、季节与角度变体。
9. `art_prompt/art_storyboard_video.md`：视频模型需要注入的固定风格标签。
10. `driector_skills/director_planning_style.md`：全片色调、光影、质感、声音与叙事规划。
11. `driector_skills/director_storyboard.md`：分镜生成的情绪、场景、镜头与提示词规范。
12. `driector_skills/director_storyboard_table_style.md`：分镜表的动作、空间、连续性、环境动态与运镜约束。

现有常规手册约 1500 行，统一采用“风格锚点 → 分类矩阵 → 可执行提示词模板 → 必守/严禁规则”的写法。本任务将综合参考：

- `3D_anime_render`：角色、资产、场景与导演技法的完整动画结构。
- `2D_90s_japanese_anime`：年代质感、旧介质色彩和时代错位规避方式。
- `realpeople_modern_city`：真实生活痕迹、物理光源、动作轨迹、空间连续性和摄影语言。

## 预计修改文件

- 新增目录 `data/skills/art_skills/3D_90s_northeast_anthropomorphic_cat/`。
- 在该目录新增上述 12 个 Markdown 文件。
- 新增 `images/cover.png`，作为视觉手册列表封面与风格参考图。
- 新增 `tests/visualManual.90sNortheast.test.mjs`，校验文件结构、核心人物/地点、60 秒时长、年代与视觉硬约束。
- 更新 `DEV_REPORT.md`：记录实际内容、验证结果、风险和回滚方式。
- 更新 `CODEX_REVIEW.md`：记录范围自查、时代一致性检查和遗留风险。
- 不修改现有视觉手册、路由、数据库结构、供应商或其他业务代码。

## 内容实施原则

1. 将用户给定的系列定位、场景、人物与色彩体系写成可复用的全局锚点。
2. 角色基础手册锁定五名角色的物种、毛色、脸部标记、体型、服装与随身物。
3. 角色衍生仅允许季节、居家/上学/劳动/节庆等有剧情依据的变化，角色识别核心不变。
4. 道具与场景按 1988—1990 年东北国企家属院语境分类，并建立“允许/慎用/禁止”年代矩阵。
5. 分镜与视频规则锁定 9:16、生活化喜剧节奏、冷暖对比、微表情和物理可执行动作。
6. 单集总时长按约 60 秒规划，镜头数量和节奏必须服务于 60 秒竖屏短片，不按 90 秒结构展开。
7. 每集结尾优先使用生活化反应或动作笑点，不通过强煽情、旁白总结或字幕说教收尾。
8. 不新增依赖，不改变现有功能，不使用 mock 冒充真实加载结果。

## 验收计划

1. 校验新目录正好包含 Toonflow 识别的 12 个字段文件，路径与命名正确。
2. 校验 `README.md` 第一行可作为视觉手册名称被 `getVisualManual` 读取，并确认封面图片可被界面加载。
3. 检查五名固定角色与七类核心地点均有明确视觉锚定。
4. 检查年代禁用项、9:16、单集约 60 秒、固定色盘、冷暖光和生活使用痕迹均进入全局与专项规则。
5. 运行 Markdown/结构一致性检查，确保无空文件、无错误路径、无遗留模板词。
6. 运行 `yarn lint`，确认新增手册未影响 TypeScript 项目。
7. 启动 Toonflow，并在视觉手册列表中确认新手册可见、12 个编辑页签均能加载。

## 风险与回滚

- 风险：模型可能把“90 年代”泛化为港风、日式昭和或现代复古影棚；通过东北地点锚点、允许物件表和负向约束控制。
- 风险：拟人猫可能漂移成真人猫耳角色、Q 版宠物或完全写实四足猫；通过角色结构、站姿/猫爪、头身比和严禁项控制。
- 风险：年代物件可能发生时间穿帮；通过 1988—1990 硬时间窗和跨文件年代检查控制。
- 回滚：删除新增的单一视觉手册目录，并回退本任务在 `PLAN.md`、`DEV_REPORT.md`、`CODEX_REVIEW.md` 中新增的对应章节；无需数据库迁移。

## 监督状态

- [x] 已读取当前任务卡（本次用户需求）。
- [x] 已分析现存视觉手册的文件结构和写法。
- [x] 已确认影响范围不涉及现有业务代码和数据库。
- [x] 已获得监督流程允许新增视觉手册文件。
- [x] 新增视觉手册、封面并完成结构验证。
- [x] 运行 lint 和应用启动/界面加载验证。
- [x] 更新开发报告和代码自查。

## 目标

将本机 `doubao2api` 暴露的 `POST /v1/video/generations` 接入 Toonflow，作为可配置的内置视频供应商，并使用真实模型标识 `doubao-video`。

## 已确认的接口契约

- 默认服务地址：`http://127.0.0.1:9090`
- 请求：`POST /v1/video/generations`
- 请求体：`model`、`prompt`、`ratio`
- 响应视频地址：`data[0].video_url`
- API Key 可选；配置后使用 `Authorization: Bearer <key>`
- 当前后端只支持文生视频，因此 Toonflow 中只声明 `text` 模式。

## 影响范围

1. 新增供应商适配器，不修改 Toonflow 通用视频生成链路。
2. 启动数据修复阶段从随安装包发布的供应商文件中注册该供应商。
3. 增加契约测试与接入文档。
4. 不修改当前工作区已有的其他未提交文件。

## 实施顺序

1. 先新增供应商契约测试并确认测试因实现缺失而失败。
2. 实现请求地址归一化、可选鉴权、请求体映射、响应解析和错误提示。
3. 增加内置供应商注册。
4. 运行契约测试、TypeScript lint、构建和启动检查。
5. 在本机 `doubao2api` 服务可用时执行真实 API 冒烟测试。

## 验收标准

- Toonflow 供应商列表出现“豆包网页视频”。
- 模型列表出现“豆包网页视频生成（自动路由）”，模型标识为 `doubao-video`。
- 模型只支持文生视频，比例透传 `16:9` 或 `9:16`。
- 成功响应能提取视频 URL，失败响应提供可定位的错误信息。
- 契约测试、lint、构建均通过，应用能够启动。

## ChatGPT 网页 GPT-5.6 SOL 主工作目录接入

### 目标

在当前 `feat/doubao-video-provider` 工作目录中保留豆包视频任务，同时接入已经在
独立分支验证通过的 `chatgptWebSol` 文本供应商，使 Toonflow 启动时能够检测和
注册该供应商。

### 影响范围

- 新增 `data/vendor/chatgptWebSol.ts`。
- 在 `src/lib/fixDB.ts` 的外部内置供应商列表中追加
  `chatgptWebSol.ts`，保留 `doubaoWeb.ts`。
- 新增契约测试和配置文档。
- 重新构建 `data/serve/app.js`，同时保留豆包和 ChatGPT 注册逻辑。
- 不修改 Toonflow 通用文本生成链路、数据库结构或现有供应商。

### 验收步骤

1. 运行 ChatGPT 和豆包供应商契约测试。
2. 运行 TypeScript lint 和生产构建。
3. 使用临时数据库启动 Toonflow，确认 `doubaoWeb` 与
   `chatgptWebSol` 均能注册且默认关闭。
4. 通过主工作目录中的真实 `chatgptWebSol.ts`、`@ai-sdk/openai` 和
   `generateText` 调用本机 `chatgpt2api`，要求返回非空文本。
5. 不把代理 API Key 写入源码、测试、文档或日志。

### 安全边界

- 不直接改写用户现有数据库；检测验收使用临时数据库。
- Toonflow 只配置 `CHATGPT2API_AUTH_KEY`，不保存 ChatGPT Access Token、
  Cookie 或 OAuth 凭据。
- 真实请求内容会由本机 `chatgpt2api` 转发至 ChatGPT 网页服务。

## Toonflow 启动时编排本地 API 服务

### 目标

无论通过后端开发模式、生产模式还是 Electron GUI 启动 Toonflow，都先并行确保
`chatgpt2api` 与 `doubao2api` 已启动，并使用 Toonflow 中保存的供应商地址和
API Key 请求各自的 `/v1/models`。只有两个服务均可访问且包含 Toonflow 配置的
模型标识时，才继续启动 Toonflow 服务。

### 影响范围

- 新增独立的本地 API 生命周期模块，负责服务探测、Conda 启动、限时重试、
  模型列表校验和子进程清理。
- 修改 `src/app.ts`，在 HTTP 服务监听前执行统一的伴随服务检查，并在 Toonflow
  关闭时清理仅由本次启动创建的子进程。
- 为本地目录、Conda 可执行文件和环境名提供环境变量覆盖；默认使用同级目录
  `../chatgpt2api`、`../doubao2api`，优先复用现有
  `chatgpt2api-py313` 与 `base` Conda 环境。
- 增加启动编排单元测试和 README 启动说明；不修改两个供应商的生成请求逻辑，
  不新增依赖，不改数据库结构。
- 重新构建 `data/serve/app.js` 与 `build/main.js`，确保生产和 Electron 启动入口
  同样包含该行为。

### 行为约束

1. 两个服务并行检查；已经可用的服务只做模型校验，不重复启动，也不在退出时关闭。
2. 服务不可达时使用 Conda 在对应项目目录启动：
   - `chatgpt2api`：`conda run --no-capture-output -n chatgpt2api-py313 python main.py`
   - `doubao2api`：`conda run --no-capture-output -n base python -m doubao2api`
3. `/v1/models` 鉴权读取 Toonflow 供应商配置，但日志和错误中禁止输出 API Key。
4. `chatgpt2api` 必须包含 `gpt-5.6-sol-wm`，`doubao2api` 必须包含
   `doubao-video`；缺失、鉴权失败或超时均阻止 Toonflow 启动。
5. Toonflow 退出时，只终止由当前 Toonflow 进程启动的伴随服务，不影响用户预先
   启动的服务。

### 测试与验收

1. 先补充单元测试，覆盖：已有服务不重复启动、不可达时并行拉起、Bearer 鉴权、
   模型缺失、超时失败、只清理自有子进程。
2. 运行新增测试及两个现有供应商契约测试。
3. 运行 `yarn lint` 和 `yarn build`。
4. 停止本次会话中的两个本地 API 后启动 Toonflow，验证其能自动拉起两者并确认
   模型；随后关闭 Toonflow，验证自有子进程同步退出。
5. 再以两个 API 已在运行的场景启动 Toonflow，验证不会重复创建或误杀外部进程。

## ChatGPT 网页 Image2 图片模型接入

### 目标

在现有 `chatgptWebSol` 供应商中增加 `gpt-image-2` 图片模型，使 Toonflow
能够通过本机 `chatgpt2api` 完成文生图、单图编辑和多参考图编辑，并保持原有
GPT-5.6 SOL 文本模型行为不变。

### 已确认的接口契约

- 模型标识：`gpt-image-2`。
- 文生图：`POST /v1/images/generations`，JSON 请求。
- 图生图/多参考图：`POST /v1/images/edits`，使用后端已支持的 JSON
  `images[].b64_json` 输入。
- 公共字段：`model`、`prompt`、`n=1`、`size`、
  `response_format=b64_json`。
- 成功结果优先读取 `data[0].b64_json`，兼容 `data[0].url`。
- `GET /v1/models` 按 `chatgpt2api` 契约只列实时文本模型，不包含图片兼容
  模型，因此不能用它声明 Image2 不可用。

### 影响范围

- 将 `chatgptWebSol` 供应商版本从 `1.0` 升至 `1.1`，模型列表增加
  “GPT Image 2”，能力模式为文生图、单图和多参考图。
- 实现图片请求地址归一化、Bearer 鉴权、比例到图片尺寸映射、参考图 JSON
  编码、错误解析以及 base64/URL 结果提取。
- 保留 GPT-5.6 SOL 固定 `xhigh` 的文本请求，不修改 Toonflow 通用图片链路。
- 更新供应商契约测试和配置文档；不新增依赖，不改数据库结构。
- 重新生成生产与 Electron 构建产物。

### 测试与验收

1. 先扩展供应商契约测试并确认实现前失败。
2. 覆盖模型声明、文生图请求、单图/多图编辑、尺寸映射、鉴权、
   base64/URL 响应和错误响应。
3. 运行 ChatGPT、伴随服务和豆包供应商测试。
4. 运行 `yarn lint` 与 `yarn build`。
5. 启动 Toonflow，确认供应商模型列表出现
   `chatgptWebSol:gpt-image-2`，并通过不触发付费生成的配置/路由检查。
6. 不自动执行真实图片生成；真实生成会消耗上游资源，需单独人工确认。

---

# 豆包网页视频改为多图参考模式

## 目标

配合 `doubao2api` 的多图参考视频接口，把 Toonflow 中的
`doubaoWeb:doubao-video` 从纯文生视频改为仅多图参考视频。模型选择界面只展示
“多图参考”模式，允许上传 1 至 10 张图片，不再提供“文生视频”选项。

## 已确认的接口契约

- 请求：`POST /v1/video/generations`。
- 请求体继续包含 `model`、`prompt`、`ratio`，并新增必填的 `images` 数组。
- `images` 包含 1 至 10 个对象，每个对象使用 `b64_json`、`filename`、
  `mime_type`；适配器从 Toonflow 的图片 `referenceList` 转换。
- 多图模式声明为 `[["imageReference:10"]]`，不再声明 `text`。
- 适配器在发请求前同时校验至少 1 张、最多 10 张，并拒绝非图片参考。
- 成功响应继续读取 `data[0].video_url`，现有鉴权和错误提示保持不变。

## 预计修改文件

- `data/vendor/doubaoWeb.ts`：更新模型名称、说明、版本和多图模式声明，校验并
  序列化最多 10 张参考图。
- `tests/doubaoWeb.vendor.test.mjs`：先更新契约测试，覆盖模型声明、1/10/11 张
  图片、请求体转换和空图片拒绝。
- `docs/doubao-video-api.md`：同步多图模式、请求格式和不再支持文生视频的说明。
- `data/serve/app.js`：通过现有构建流程更新生产服务产物。
- `DEV_REPORT.md`、`CODEX_REVIEW.md`：记录实际变更、验证结果、风险及回滚方案。

## 实施与验收

1. 先更新供应商测试并确认旧实现无法满足新契约。
2. 实现仅多图模式和图片输入转换，不改 Toonflow 通用视频生成链路。
3. 运行豆包供应商测试、伴随服务测试、`yarn lint` 和 `yarn build`。
4. 启动 Toonflow，确认模型下拉只有多图参考模式，且上传区限制为 10 张。
5. 使用拦截请求或测试服务验证完整请求，不自动触发真实视频生成。
6. 测试失败最多自动修复两轮；仍失败则停止并报告，不绕过测试。
7. 完成开发报告与代码自查，创建 PR 后等待人工合并，不自动合并。

## 当前状态

- [x] 完成 Toonflow 模型声明、参考图结构和供应商请求的影响分析。
- [x] 明确下线纯文生视频是本次需求要求的有意行为变化。
- [x] 获得监督流程允许修改业务代码的人工确认。
- [x] 先补充/更新测试。
- [x] 实现业务代码并更新 API 文档。
- [x] 完成 lint、构建、启动验证、开发报告和代码自查。
- [ ] 创建 PR，等待人工合并。

---

# 图片编辑器左侧工具栏保存按钮

## 当前任务卡

- 目标：在 `#/production` 图片流程编辑器的左侧工具栏增加一个清晰可见的
  “保存”按钮。
- 背景：现有保存入口位于各图片节点内部，画布缩小时只有约十几个像素，
  用户难以发现；左下角 Vue Flow 控件的最后一个图标实际是锁定/解锁画布。
- 保存语义：先选中一个包含图片的节点，再点击工具栏“保存”；保存当前节点与
  连线，并把该节点图片设为对应分镜或衍生资产的最终结果。
- 兼容要求：保留现有节点内“选取”按钮、上传、生成、自动排版、缩放、
  画布锁定和关闭确认行为，不删除或替换已有功能。

## 影响范围分析

1. 当前运行中的 `Toonflow-app` 只包含编译后的
   `data/web/index.html`；可维护的 Vue/TypeScript 源码位于官方独立仓库
   `HBAI-Ltd/Toonflow-web`。
2. 源码改动限定在图片编辑器工具栏组件及必要的多语言提示；不修改其他页面、
   通用画布组件或视频工作台。
3. 新按钮复用现有 `sureNode(imageUrl)` 链路以及
   `/production/editImage/saveImageFlow`、
   `/production/editImage/updateImageFlow`，不新增接口、不修改数据库结构。
4. 保存前要求恰好选中一个带图片的 upload/generated 节点；未选、选中多个或
   节点无图片时只给出提示，不发起写请求。
5. 保存成功后沿用现有行为：父级分镜/资产记录接收 `imageUrl` 与 `flowId`，
   编辑器关闭。这样不会产生未关联的 `o_imageFlow` 记录。
6. 当前 `Toonflow-app` 工作树包含其他任务的未提交修改；本任务不得覆盖、
   格式化或提交那些无关文件。

## 预计修改文件

### Toonflow-web 源码仓库

- `src/views/production/components/editImage/index.vue`
  - 左上工具栏增加带文字和保存图标的按钮。
  - 从选中节点解析图片 URL，并复用现有保存函数。
  - 增加保存中的重复点击保护。
- `src/locales/language/*.json`
  - 增加“请选择一个要保存的图片节点”等必要提示，保持现有多语言能力。
- `tests/editImageToolbarSave.test.mjs`
  - 增加无新依赖的源码契约测试，先验证旧实现缺少工具栏保存入口，再验证新入口、
    单选约束和现有保存链路复用。
- `package.json`
  - 仅在需要暴露上述测试命令时增加测试脚本，不新增依赖。

### 当前 Toonflow-app 集成仓库

- `data/web/index.html`
  - 使用 Toonflow-web 正式构建产物更新，不直接手改压缩代码。
- `tests/editImageToolbarSave.test.mjs`
  - 校验内置前端产物包含工具栏保存入口和选中节点保存契约。
- `PLAN.md`、`DEV_REPORT.md`、`CODEX_REVIEW.md`
  - 记录本任务计划、实际变更、验证结果、风险和回滚方式。

## 实施顺序

1. 在独立的 Toonflow-web 功能分支中补充契约测试，并确认旧实现不能通过。
2. 实现工具栏保存按钮、选中节点校验和防重复提交，不改后端接口。
3. 运行前端契约测试、`type-check` 与生产构建。
4. 将构建产物集成到当前 Toonflow-app 的 `data/web`，补充产物契约测试。
5. 运行 Toonflow-app 相关测试、`yarn lint`，确认服务可以启动。
6. 在当前 `http://127.0.0.1:10588/#/production` 页面验证按钮可见；
   用未选节点场景验证无写入提示，不改动用户现有分镜结果。
7. 更新 `DEV_REPORT.md` 和 `CODEX_REVIEW.md`，创建 PR 后等待人工合并，
   不自动合并。

## 验收标准

- 图片编辑器左上工具栏显示带“保存”文字和保存图标的按钮，不再依赖用户寻找
  节点内的小图标。
- 选中且仅选中一个带图片的节点时，按钮调用现有保存链路。
- 未选、选中多个或节点没有图片时不发请求，并显示明确提示。
- 保存期间重复点击不会重复提交。
- 原节点内“选取”入口及其他画布功能保持可用。
- 前端测试、类型检查、生产构建、Toonflow-app 产物测试与 lint 均通过，
  页面能够启动。

## 风险与回滚

- 风险：前端源码和后端内置产物分属两个仓库，必须保证源提交与
  `data/web/index.html` 构建产物对应，避免只修源码或只修产物。
- 风险：当前 Toonflow-app 工作树含无关未提交修改，提交和 PR 必须严格限定
  本任务文件。
- 风险：保存会更新用户选中的分镜/衍生资产结果；自动验证不得在用户现有项目上
  执行成功保存，只验证可见性和无选中节点的无写入分支。
- 回滚：回退 Toonflow-web 的工具栏组件、提示和测试提交，并恢复
  Toonflow-app 原 `data/web/index.html`；不需要数据库迁移。

## 监督状态

- [x] 已读取当前任务卡（本次用户需求）。
- [x] 已确认当前服务实际运行目录和非主分支状态。
- [x] 已完成前端源码、保存链路、后端接口和数据关联影响分析。
- [x] 已输出本任务 `PLAN.md`。
- [ ] 等待监督流程允许修改业务代码。
- [ ] 先补充/更新测试。
- [ ] 实现业务代码并构建集成产物。
- [ ] 运行测试、lint 和页面启动验证。
- [ ] 更新开发报告、代码审查并创建 PR。
