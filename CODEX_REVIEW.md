# 治愈系都市女性生活视觉手册与导演手册自查

## 结论

未发现阻止交付的问题。两套手册均符合 Toonflow 的独立加载结构，并在真实
应用界面中显示为可选卡片；自动化和类型检查通过。

## 范围

- 分支从 `master` 创建，不直接修改 `main/master`。
- 只新增两套手册、封面、专项测试和任务文档。
- 未修改既有手册、业务代码、前端、接口、数据库、登录、权限、支付、安全、
  删除或批量操作。
- 未引入依赖、未执行无关重构、未自动合并 PR。

## 正确性

- 视觉手册包含固定的 12 个字段，导演手册包含固定的 3 个字段和封面。
- 两套手册共享 3:4、68—89 秒、约 80 秒、20—27 节拍和声音安全指标。
- 六类故事、五名人物、四类空间与时间戳行动链完整。
- 明确禁止恋爱、霸总、暧昧、死亡疾病苦情、同脸、年龄漂移、手指错误、
  道具穿模、时间戳倒退、过度溶解、水印、样板间和 9:16 黑边。
- 封面无文字、Logo、水印和黑边，年轻女性与外婆的年龄和体态差异清楚。

## 验证

- 测试在目标目录不存在时先失败，证明可捕获缺文件和缺封面。
- 专项测试 10/10 通过。
- 独立 PR 工作树 MJS 回归 10/10 通过。
- `yarn lint` 通过。
- `git diff --check` 通过。
- 当前运行中的 Toonflow 返回 HTTP 200，UI 同时显示两张新增手册卡片。

## 风险

- 两套手册包含必要的共同导演规则，后续维护要同步公共常量。
- 单张封面不是六类故事的完整视觉样本。
- 生成模型仍可能发生手部、年龄和跨镜连续性错误，需要逐镜质检。
- 参考音频语义没有被伪造为分析结论。

## 回滚

删除两个新增手册目录、专项测试和三份任务文档。无数据迁移和用户数据恢复。

## 合并

仅创建草稿 PR，等待人工审查和合并，不自动合并。
