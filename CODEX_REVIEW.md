# Codex 自查：手动图片/视频生成模式

## 结论

未发现阻止合入的问题。可见媒体入口不再调用图片/视频模型，旧接口被 409 拦截，
底层 `Ai.Image` 和 `Ai.Video` 在供应商解析前失败关闭；文本模型能力保持不变。

## 检查项

- 独立分支开发，未修改主分支。
- 没有无关重构、依赖或数据库结构变化。
- 图片复用既有上传/保存逻辑。
- 视频上传验证项目、剧本和轨道归属，写入既有表并选中原轨道。
- 单项和批量提示词均包含生成参数、参考素材说明、连续性和负面约束。
- 新测试先红后绿；后端测试、lint、构建和启动检查通过。
- 前端测试与生产构建通过；完整类型检查的既有失败已如实记录。

## 剩余风险

- 大视频上传受现有请求大小限制。
- 真实项目上传尚未执行，以避免未经确认写入用户数据。
- 前端仓库既有类型错误需单独治理。

---

# Codex 自查：豆包网页视频 API 接入

## 范围

本次仅新增豆包网页视频供应商、启动注册、契约测试和 API 文档。未改动
Toonflow 通用视频生成流程、数据库结构、登录权限逻辑、前端页面或其他
供应商实现。

## 自查结论

- 使用独立分支 `feat/doubao-video-provider`，未直接修改 `master`。
- 模型显示名与 `doubao2api /v1/models` 一致，模型标识为
  `doubao-video`。
- 只声明后端真实支持的 `text` 视频模式，未提供虚假的参考图或音频能力。
- 请求仅发送 `model`、`prompt`、`ratio`，与现有后端契约一致。
- API Key 为空时不发送 Authorization；填写时会去除重复的 Bearer 前缀。
- 服务根地址和完整接口地址均不会产生重复路径。
- 非 JSON、HTTP 失败、缺少视频 URL 和豆包人工验证均有明确错误。
- 视频 URL 交给 Toonflow 既有 `AiVideo` 下载保存流程处理。
- 新供应商源码随 `data` 资源打包，并由启动修复流程注册。
- 单元测试、TypeScript lint 和构建均通过。
- 实际 `doubao2api` 服务可用、账号已登录、模型存在，但真实视频生成被
  上游 `710022004` 人工验证阻塞；没有伪造通过结果，也没有自动绕过风控。

## 已知限制

- 真实端到端生成仍需用户完成人工验证后复测。
- 供应商默认不启用，符合项目对第三方供应商的既有默认策略。
- `data/serve/app.js` 是项目跟踪的构建产物，需要与源码变更一起保留。

## ChatGPT 网页 GPT-5.6 SOL 主工作目录自查

### 结论

未发现阻止合入的问题。ChatGPT 供应商已经存在于当前
`feat/doubao-video-provider` 工作目录的源码、注册列表和生产 bundle 中；
运行中的 Toonflow 开发服务已检测并注册该供应商，真实模型请求返回
`MAIN_TOONFLOW_OK`。

### 检查项

- 没有直接修改 `master`。
- `externalBuiltInVendorFiles` 同时保留 `doubaoWeb.ts` 和
  `chatgptWebSol.ts`。
- ChatGPT 供应商只声明一个真实文本模型 `gpt-5.6-sol-wm`。
- 最终请求固定使用 `reasoning_effort: xhigh`。
- API Key 会移除重复的 `Bearer ` 前缀，错误信息不输出凭据。
- 不读取 ChatGPT Access Token、Cookie 或 OAuth 数据。
- ChatGPT 与豆包契约测试共同通过，lint 和生产构建通过。
- `data/serve/app.js` 与 `src/lib/fixDB.ts` 的注册列表一致。

### 剩余风险

- 供应商默认关闭且没有写入 API Key，用户完成配置后才能由 Toonflow Agent 使用。
- 代理 API Key 使用 Toonflow 既有 SQLite 存储机制。
- 上游网页模型与协议可能变化；持续网络故障仍会明确失败。
- 当前工作目录包含其他尚未提交改动，提交时必须只选择本任务和用户明确要保留的
  相关文件，不能误带无关供应商修改。

## Toonflow 伴随 API 启动编排自查

### 结论

未发现阻止交付的问题。后端开发、生产 bundle 和 Electron GUI 共用
`startServe`，因此统一执行两个伴随 API 的启动与模型检查。真实冷启动、
外部服务复用和正常关闭回收均已验证。

### 检查项

- 工作分支为 `feat/companion-api-startup`，未直接修改 `master`。
- 两个服务使用 `Promise.allSettled` 并行检查，单项失败时会等待另一项收敛
  并统一清理本次创建的进程。
- HTTP 401/403、无效 JSON 和模型缺失均立即失败；连接失败和 HTTP 5xx
  在限定时间内重试。
- ChatGPT 与豆包 API Key 会去除重复的 `Bearer ` 前缀，并仅通过请求头或
  子进程环境变量使用。
- 日志仅包含服务名、Conda 环境名和模型标识，不包含 API Key。
- 子进程使用独立进程组；正常关闭时先发送 `SIGTERM`，超时后升级为
  `SIGKILL`。
- 只记录由当前 Toonflow 创建的子进程，预先存在的服务不会被关闭。
- CLI 启动 Promise 已显式处理拒绝，失败后不会留下仅初始化数据库但长期
  挂起的 Node 进程。
- Electron 启动检查失败时显示错误并退出，不创建不可用主窗口。
- 单元测试、供应商契约测试、TypeScript lint、生产构建和真实端口验收
  全部通过。

### 剩余风险

- 强制 `SIGKILL` 或系统崩溃不能触发应用内清理，可能留下孤立的 Python
  服务；再次启动时会探测并复用这些服务，不会重复启动。
- 豆包 `/v1/models` 是能力列表检查，不保证当前网页账号已登录或真实生成
  不触发风控。
- 打包应用依赖外部 Python 项目和 Conda，部署时必须提供目录与环境变量。
- 当前工作区还包含启动数据库类型生成及既有供应商带来的未提交换行改动，
  本任务没有清理或覆盖这些用户变更。

## ChatGPT 网页 Image2 接入自查

### 结论

未发现阻止交付的问题。Image2 复用现有 `chatgptWebSol` 的服务地址和
代理 API Key，没有增加新的凭据或数据库结构；运行态供应商列表已经显示
`gpt-image-2`。

### 检查项

- 供应商版本已从 `1.0` 升至 `1.1`，文本模型仍为
  `gpt-5.6-sol-wm` 且固定 `xhigh`。
- 图片模型标识与 `chatgpt2api` 契约一致，为 `gpt-image-2`。
- 模型能力只声明后端真实支持的文生图、单图和多参考图。
- 文生图与编辑分别使用 `/v1/images/generations` 和
  `/v1/images/edits`，不会混用文本接口。
- base64 data URL 会拆分 MIME 与纯数据；裸 base64 默认按 PNG 处理。
- API Key 会清理重复的 `Bearer ` 前缀，仅放入 Authorization 请求头。
- 错误响应不会拼接请求头、请求体或 API Key。
- 返回 URL 会交给 Toonflow 既有下载逻辑，base64 会补齐 PNG data URL 头。
- 未更改通用图片生成、登录权限、支付、数据库结构或其他供应商逻辑。
- 相关测试、lint、构建和运行态模型列表检查均通过。

### 剩余风险

- 未执行会产生资源消耗的真实图片生成，无法在本轮确认当前图片账号额度及
  ChatGPT 网页风控状态。
- `/v1/models` 不列图片模型，启动时只能验证 ChatGPT 文本模型和 API
  鉴权；图片能力失败会在实际图片请求时明确返回。
- 画面比例会映射到 Image2 固定尺寸，Toonflow 的分辨率档位不会直接控制
  Image2 输出像素。

---

## 豆包网页仅多图视频接入自查

### 结论

未发现阻止交付的问题。供应商模型已移除 `text`，只暴露最多 10 张图片的
多参考模式；适配器、doubao2api 请求校验、账号内上传和浏览器附件载荷之间的
字段契约一致。

### 检查项

- 供应商版本为 `2.1`，模型标识仍为 `doubao-video`。
- 模型模式精确为 `[["imageReference:10"]]`，没有残留文生视频选项。
- 图片列表缺失、超过 10 张或包含视频/音频参考时，在调用 fetch 前失败。
- Data URL 只接受 `image/*;base64`，裸 Base64 使用稳定的 PNG 元数据。
- API Key 仍只写入 Authorization，请求日志只记录模型、比例和图片数量。
- 成功响应继续读取 `data[0].video_url`，错误和风控提示行为保持。
- 未修改 Toonflow 通用视频 UI、数据库结构、登录、权限或其它供应商。
- 供应商测试、伴随服务测试、TypeScript lint、生产构建和三服务冷启动通过。

### 剩余风险

- 当前豆包账号未登录，真实多图上游生成尚未验证。
- Toonflow 允许的参考图实际字节大小仍受浏览器内存、HTTP 请求体和
  doubao2api 部署限制影响。
- 现有数据库中已保存的旧 `text` 模式任务配置不会被批量改写；模型界面重新
  选择该供应商时只会提供多图模式。

### 结论

静态合同、自动化测试、构建和运行态服务检查均通过。真实上游生成属于需要
账号登录与额度授权的剩余人工验收，不应自动执行。

---

## 东北90年代拟人猫视觉手册自查

### 结论

未发现阻止交付的问题。新手册使用 Toonflow 现有 12 字段目录约定，不修改
路由、数据库、前端组件或通用生成链路；运行中的应用已能读取所有字段和封面。

### 范围检查

- 工作分支为 `feat/companion-api-startup`，不是 `main/master`。
- 仅新增独立视觉手册目录、专项测试，并在既有任务文档中追加本任务章节。
- 未修改或删除任何现存视觉手册。
- 未引入依赖，未修改登录、权限、支付、安全、数据库或用户数据。
- 未触发真实角色、场景或视频生成。

### 内容检查

- 1988—1990 年、北安市、红星机械厂家属院在全局与专项手册中一致。
- “90年代”与时长已明确区分；单集固定为约 60 秒、9:16 竖屏。
- 50% 家庭和伙伴喜剧、30% 年代回忆、20% 温情成长进入导演规划、视频与
  分镜表约束。
- 五名固定角色均有不可变识别层；角色衍生明确“身份不变、毛色不变、脸部
  标记不变、头身比例不变”。
- 七个核心地点均有固定结构、色彩、材质、使用痕迹和现代元素禁令。
- 道具手册按家庭、厨房、学校、儿童游戏、工厂、市场、影音和铁路分类，并把
  不确定品牌/型号设为慎用。
- 喜剧来自角色关系、空间站位和物件动作；尾梗保留 2—4 秒，不使用强行煽情、
  字幕说教和夸张动画鬼脸。
- 封面符合手册色盘和时代环境，没有文字、Logo、水印或现代物件。

### 自动化与运行态检查

- 专项测试 8/8 通过。
- TypeScript lint 通过。
- 12 个 Markdown 字段全部非空，总计 2304 行。
- API 返回 12/12 非空字段。
- Toonflow 列表、编辑弹窗、分镜视频页签、导演技法页签和封面均加载成功。

### 剩余风险

- 单一封面不能替代五名角色的正式四视图与跨镜一致性压力测试。
- 生成模型对猫爪、尾巴、棉服厚度和儿童体型仍可能出现偶发漂移。
- 1988—1990 年真实品牌、包装、节目或游戏型号若进入具体剧本，需要额外史料
  核验。
- 当前工作区存在其他任务的未提交变更，必须使用隔离分支或精确暂存，避免 PR
  混入无关文件。

### 回滚

本任务为独立目录新增，删除该视觉手册目录和专项测试即可完成主要回滚；文档章节
可单独回退，无数据库迁移或数据修复。
