# 图片节点

在画布中展示图片，可作为角色、场景、道具或分镜参考。

## 输入与输出

- 导入本地图片文件，单个文件不能为空且不超过 100 MB。
- 输出当前图片素材，可连接图片生成、视频生成等支持图片输入的节点。

## 使用方法

1. 点击上传按钮，选择图片文件。
2. 上传完成后，节点按图片比例显示预览。
3. 将图片输出连接到需要参考图的节点。
4. 选中节点后，使用顶部工具栏下载、预览或替换图片。
