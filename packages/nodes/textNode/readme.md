# 文本节点

编写、保存和生成文本，可用于剧本、分镜说明、提示词或素材分析。

## 输入与输出

- 输入支持文本、图片和视频参考。
- 输出节点当前的完整文本，可连接支持文本输入的下游节点。

## 使用方法

1. 点击“编辑”直接输入或修改正文。
2. 需要 AI 生成时，选中节点，在下方输入要求并选择模型；没有模型时先在设置中添加。
3. 可连接参考节点，在提示词中说明如何使用这些参考内容。
4. 点击生成按钮，文本会逐步显示；新生成的内容会替换当前文本输出。
5. 生成结束后可继续手工编辑，再将文本输出连接到后续制作节点。

图片与视频参考的理解能力取决于所选模型。
